{
    "configurations": [
        {
            "name": "ALL",
            "includePath": [
                "${default}",  // get includePath set in workspace (or user)
                "${workspaceFolder}/source/**"
            ]
        }
    ],
    "version": 4
}