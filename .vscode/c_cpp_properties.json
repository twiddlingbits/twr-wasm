{
    "configurations": [
        {
            "name": "ALL",
            "includePath": [
                "${default}",  // get includePath set in workspace (or user)
                "${workspaceFolder}/include",
                "${workspaceFolder}/twr-bigint",
            ]
        }
    ],
    "version": 4
}